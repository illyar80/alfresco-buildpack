#!/usr/bin/env bash
# bin/release <build-dir>

cat <<EOF
---
config_vars:
  JAVA_HOME: $BUILD_DIR/jdk
  JAVA_OPTS: -Xmx384m -Xss512k -XX:+UseCompressedOops
default_process_types:
  web: $BUILD_DIR/bin/startup.sh
EOF
