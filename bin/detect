#!/usr/bin/env bash
# bin/use <build-dir>

#if [ -f $1/Procfile ]; then
echo "############"
echo "## DETECT ##"
echo "############"
echo "Arg1 = $1"
ls -l $1
echo "Buildpack Alfresco" && exit 0
#else
#  echo "no" && exit 1
#fi
